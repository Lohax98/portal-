# Portal 系列游戏价格监控

监控 Steam 上 **Portal** 和 **Portal 2** 的价格，降价时通过 GitHub Issue 邮件通知你。

## 仓库地址

`https://github.com/Lohax98/portal-`

## 工作原理

- 每天 **北京时间 9:00**（UTC 1:00）通过 GitHub Actions 自动运行
- 调用 Steam Store API 查询 Portal（AppID 400）和 Portal 2（AppID 620）的国区价格
- 如果当前价格 **低于** 之前记录的价格，自动创建 Issue
- 你 Watch 了仓库后，GitHub 会发邮件到你的绑定邮箱

## 快速开始

### 1. Watch 仓库

打开仓库主页，点击右上角 **Watch** → 选择 **All Activity**。

> ⚠️ 必须 Watch 才能收到 Issue 创建的邮件通知！

### 2. 手动测试

1. 打开仓库的 **Actions** 标签页
2. 左侧选择 **Check Portal Price**
3. 点击 **Run workflow** → **Run workflow**
4. 等待执行完成，检查是否有 Issue 被创建

### 3. 验证通知

如果当前是原价（Portal 系列目前 ¥42），正常不会创建 Issue。你可以临时修改 `price_state.json` 来模拟降价测试：

```json
{
  "400": 3000,
  "620": 3000
}
```

把记录价改低后推送，再手动触发 workflow，当前价格（¥42）会高于 ¥30，不会触发。你可以继续改成 `5000`，这样当前价格就会被判定为"降价"，触发 Issue 通知。

## 文件说明

| 文件 | 用途 |
|------|------|
| `portal_monitor.py` | Python 监控脚本，查询价格并判断是否降价 |
| `.github/workflows/check_price.yml` | GitHub Actions 工作流配置 |
| `price_state.json` | 价格状态记录（脚本自动生成和更新） |
| `alert.md` | 降价通知内容（脚本自动生成） |

## 自定义

### 修改检查频率

编辑 `.github/workflows/check_price.yml`，修改 `cron` 表达式：

```yaml
schedule:
  - cron: "0 1 * * *"   # UTC 1:00，即北京时间 9:00
```

> 上海时间 = UTC + 8

### 监控其他游戏

编辑 `portal_monitor.py` 中的 `APP_IDS` 字典：

```python
APP_IDS = {
    400: "Portal",
    620: "Portal 2",
    # 添加更多游戏，AppID 可从 Steam 商店 URL 中获取
}
```

## 注意事项

- 仓库目前为 **Public**，GitHub Actions 免费额度足够
- Steam API 无需 API Key，但请注意不要频繁请求
- `price_state.json` 的 git 历史记录可以作为价格变化追踪
